/target
**/*.rs.bk
.DS_Store
# .graph/ carries repo config, plans, and tools for CI; everything else
# under it (embedded DB state, scratch) stays ignored.
/.graph/*
!/.graph/config.toml
!/.graph/plans/
!/.graph/tools/
*.db
