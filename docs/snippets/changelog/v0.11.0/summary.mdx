Track review findings as ongoing threads in the GitHub pack with new `gh_pr_review_threads` and `gh_pr_thread_sync` tools that let plans read and reconcile pull request review threads. A generated changelog page is now published with per-release summaries and migration prompts inferred by a graph plan. Release tooling has been fixed to keep the docs `release_version` in sync and prevent template syntax leakage into generated entries.
