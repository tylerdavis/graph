### Every model is now a role

Config now treats all models uniformly: the `[models.<role>]` table replaces the old fixed role slots and `[models.named]` section. This is a config format change (version 2) that requires migration.
