### Added support for config v2

Config v2 now treats all models uniformly: the `[models.<role>]` table replaces the old fixed role slots and `[models.named]` section. This is a config format change that requires a migration.  See [the Config v2 entry](#config-v2) for details.
